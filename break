# 中断记录

- `时间`: [2026-03-19 00:32:42 +0800]
- `阶段`: 阶段 5 / RFC 9000、RFC 9001 连接管理、CID 生命周期、迁移与终止
- `原因`: 用户要求立即暂停推理，并把当前上下文固化到根目录，便于之后从断点继续

## 当前目标

继续完成阶段 5 的真实端到端验证，当前尚未收口的是：

- `preferred-address` 迁移后的 `example/` 真 UDP 文件传输仍会超时
- 对应的 `topo-stage5-preferred` 还没有完成闭环验证

本阶段设计与实现必须继续严格对照：

- `rfc-docs/9000`
- `rfc-docs/9001`

## 当前已完成的阶段 5 实现

### 1. 协议主能力

以下主能力已经落盘：

- `NEW_CONNECTION_ID`
- `RETIRE_CONNECTION_ID`
- `PATH_CHALLENGE`
- `PATH_RESPONSE`
- `NEW_TOKEN`
- 协商 `idle timeout`
- `preferred_address` 传输参数编解码
- `stateless reset` 构造与识别辅助逻辑
- path-aware 的收包、发包、超时入口
- 基于 path 的 anti-amplification 记账

相关文件主要包括：

- `include/quic_recovery.h`
- `include/quic_tls.h`
- `src/tls/quic_tls.c`
- `tests/test_phase18.c`
- `tests/test_phase19.c`
- `tests/test_phase20.c`

### 2. example / topo 入口

阶段 5 的真实网络入口也已经接上：

- `example/client.c`
- `example/server.c`
- `topo.py`
- `Makefile`

已新增：

- `preferred-address` profile
- `make topo-stage5-preferred`
- 客户端、服务端的可选 preferred port 参数

## 当前已确认通过的测试

### 单元与阶段测试

以下测试已经通过：

- `make TEST_REPEAT=1 test18`
- `make TEST_REPEAT=1 test19`
- `make TEST_REPEAT=1 test20`
- `make TEST_REPEAT=1 test18 test19 test20`

### 已确认未破坏的旧路径

非迁移模式的真实 UDP 回环 example 已确认仍然正常：

- 服务端与客户端都能完成握手
- 双向文件传输成功
- 加密 `PING` 成功

这说明阶段 5 的 path-aware 改造没有把阶段 4 的基础 example 路径带坏。

## 本轮已修掉的阶段 5 关键问题

### 1. test18 自身问题

- idle timeout 测试曾错误地重置了配置状态
- 断言也曾与 RFC 9000 §10.1 不一致
- 现已按 `max(peer/local min, 3 * current PTO)` 修正

### 2. PATH 控制帧待发送判定与构包不一致

- `has_pending_output()` 可能返回真
- 实际构包又返回 `no datagram pending`
- 已补 `quic_tls_has_sendable_path_control()` 并统一到 pending / build 判定

### 3. PATH_CHALLENGE token 状态复用错误

此前一个 token 字段同时承担：

- 本端发出的 `PATH_CHALLENGE`
- 对端发来的 `PATH_CHALLENGE`

这会导致双向 path validation 互相污染。现已拆分为：

- `local_challenge_token`
- `peer_challenge_token`

### 4. PATH_RESPONSE 校验时机错误

此前依赖 `challenge_in_flight`

- ACK 先到会清掉 `challenge_in_flight`
- 同包稍后处理到 `PATH_RESPONSE` 就被错误拒绝

现已增加：

- `challenge_expected`

并据此判断 `PATH_RESPONSE` 是否可接受。

### 5. preferred_address 服务器内部路径错误

此前服务端在 post-handshake control 里会错误地把“自己的 preferred address”加成一个内部 peer path。

这对真实 socket 发送方向是错误的，已移除。

## 当前未收口的问题

### 症状

在本机真实 UDP 回环、启用 preferred-address 迁移时：

- 服务端能完成握手
- 客户端能学到 preferred address
- 客户端会开始迁移
- 但后续 stream exchange 超时

失败输出形态为：

- 客户端：
  - `client learned preferred address port=4445`
  - `client started preferred-address migration`
  - `client timeout waiting for QUIC stream exchange`
- 服务端：
  - `server listening on 127.0.0.1:4434 (preferred 4445)`
  - `server handshake complete and preferred-address mode is active`
  - `server timeout waiting for QUIC stream exchange`

### 当前判断

问题已经从协议单元逻辑收敛到“真实 socket 迁移路径”：

- 非迁移模式 example 正常
- `test18/19/20` 正常
- 仅 preferred-address 真 UDP 路径超时

最可能的方向是：

1. 客户端当前仍使用“connected UDP socket + recv()”语义，不利于迁移期间同时接收旧地址和新地址的数据报
2. 客户端在切到 preferred port 后，仍可能丢掉从旧端口到达的必要握手后续包或 ACK
3. 真正需要的是像服务端一样改成基于 `sendto()/recvfrom()` 的 path-aware 收发，而不是继续依赖单目标 `connect()`

## 下一步推荐顺序

1. 优先改 `example/client.c`
   - 从 `connect()/send()/recv()` 主路径改为 `sendto()/recvfrom()`
   - 收包时构造 `quic_path_addr_t`
   - 调 `quic_tls_conn_handle_datagram_on_path()`
   - 保证迁移期间既能接收旧 path，也能接收 preferred path

2. 再检查 `example/server.c`
   - 确认 preferred socket 与 primary socket 的收发切换是否与 `active_path_index` 一致
   - 如有必要，补更清晰的 path 调试信息，但最终收尾前要去掉临时 debug 输出

3. 修完后先跑本机回环
   - `preferred-address` loopback example

4. 本机回环通过后再跑真实拓扑
   - `make topo-stage5-preferred`

5. 收尾
   - 更新 `debug`
   - 更新 `README.md`
   - 更新 `log`

## 当前值得注意的文件

### 已改且与阶段 5 强相关

- `include/quic_recovery.h`
- `include/quic_tls.h`
- `src/tls/quic_tls.c`
- `tests/test_phase18.c`
- `tests/test_phase19.c`
- `tests/test_phase20.c`
- `example/client.c`
- `example/server.c`
- `topo.py`
- `Makefile`

### 可能还需要继续调整

- `example/client.c`
- `example/server.c`

## 当前实现中的临时调试痕迹

`example/client.c` 和 `example/server.c` 里还保留了少量阶段 5 调试输出，例如：

- `client learned preferred address ...`
- `client started preferred-address migration`
- `server handshake complete and preferred-address mode is active`

这些输出在定位阶段 5 真实迁移问题时有用，但在最终收尾前应评估是否保留。

## 一句话状态

阶段 5 的协议核心与单元测试已经基本完成，真正剩下的是 `preferred-address` 在真实 UDP/example/topo 路径上的迁移收尾，首要突破口是把客户端改成真正 path-aware 的 `sendto()/recvfrom()` 收发模型。
