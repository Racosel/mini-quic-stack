# 中断记录

- `时间`: [2026-03-19 09:35:22 +0800]
- `阶段`: 阶段 5 / RFC 9000、RFC 9001 连接管理、CID 生命周期、迁移与终止
- `原因`: 用户要求立即暂停推理，并把当前上下文固化到根目录，便于之后从断点继续

## 当前目标

继续完成阶段 5 的真实端到端验证，当前只剩最后一层真实拓扑闭环：

- `preferred-address` 的本机 loopback 文件传输已经修通
- `topo-stage5-preferred` 仍未做 root 环境复验

本阶段设计与实现必须继续严格对照：

- `rfc-docs/9000`
- `rfc-docs/9001`

## 当前已完成的阶段 5 实现

### 1. 协议主能力

以下主能力已经落盘：

- `NEW_CONNECTION_ID`
- `RETIRE_CONNECTION_ID`
- `PATH_CHALLENGE`
- `PATH_RESPONSE`
- `NEW_TOKEN`
- 协商 `idle timeout`
- `preferred_address` 传输参数编解码
- `stateless reset` 构造与识别辅助逻辑
- path-aware 的收包、发包、超时入口
- 基于 path 的 anti-amplification 记账

相关文件主要包括：

- `include/quic_recovery.h`
- `include/quic_tls.h`
- `src/tls/quic_tls.c`
- `tests/test_phase18.c`
- `tests/test_phase19.c`
- `tests/test_phase20.c`

### 2. example / topo 入口

阶段 5 的真实网络入口也已经接上：

- `example/client.c`
- `example/server.c`
- `topo.py`
- `Makefile`

已新增：

- `preferred-address` profile
- `make topo-stage5-preferred`
- 客户端、服务端的可选 preferred port 参数

## 当前已确认通过的测试

### 单元与阶段测试

以下测试已经通过：

- `make TEST_REPEAT=1 test18`
- `make TEST_REPEAT=1 test19`
- `make TEST_REPEAT=1 test20`
- `make TEST_REPEAT=1 test18 test19 test20`
- `make test18 test19 test20`

### 已确认未破坏的旧路径

非迁移模式的真实 UDP 回环 example 已确认仍然正常：

- 服务端与客户端都能完成握手
- 双向文件传输成功
- 加密 `PING` 成功

这说明阶段 5 的 path-aware 改造没有把阶段 4 的基础 example 路径带坏。

## 本轮已修掉的阶段 5 关键问题

### 1. test18 自身问题

- idle timeout 测试曾错误地重置了配置状态
- 断言也曾与 RFC 9000 §10.1 不一致
- 现已按 `max(peer/local min, 3 * current PTO)` 修正

### 2. PATH 控制帧待发送判定与构包不一致

- `has_pending_output()` 可能返回真
- 实际构包又返回 `no datagram pending`
- 已补 `quic_tls_has_sendable_path_control()` 并统一到 pending / build 判定

### 3. PATH_CHALLENGE token 状态复用错误

此前一个 token 字段同时承担：

- 本端发出的 `PATH_CHALLENGE`
- 对端发来的 `PATH_CHALLENGE`

这会导致双向 path validation 互相污染。现已拆分为：

- `local_challenge_token`
- `peer_challenge_token`

### 4. PATH_RESPONSE 校验时机错误

此前依赖 `challenge_in_flight`

- ACK 先到会清掉 `challenge_in_flight`
- 同包稍后处理到 `PATH_RESPONSE` 就被错误拒绝

现已增加：

- `challenge_expected`

并据此判断 `PATH_RESPONSE` 是否可接受。

### 5. preferred_address 服务器内部路径错误

此前服务端在 post-handshake control 里会错误地把“自己的 preferred address”加成一个内部 peer path。

这对真实 socket 发送方向是错误的，已移除。

### 已收口的问题

此前本机真实 UDP 回环、启用 preferred-address 迁移时，会出现：

- 客户端学到 preferred address 并开始迁移后超时
- 服务端握手完成后也一直等不到 stream exchange

这个问题已经修复，根因与方案如下：

- 根因：`example/client.c` 使用单个 connected UDP socket，并在 `send_pending_packets()` 内按每个包反复 `connect()`；发送 path 与接收 peer 过滤被错误耦合
- 修复：只保留最初一次 `connect()` 用于获取本地地址，随后 `AF_UNSPEC` 断开，并改为 `sendto()/recvfrom()` + `quic_tls_conn_handle_datagram_on_path()`
- 结果：本机 preferred-address 文件传输 loopback 已连续 5 轮通过

## 下一步推荐顺序

1. 在 root 环境执行真实拓扑验证
   - `make topo-stage5-preferred`

2. 如果拓扑通过
   - 更新 `README.md`，把阶段 5 标记为完成
   - 评估是否保留 `example/client.c` 与 `example/server.c` 的临时调试输出
   - 更新 `debug` 与 `log`

3. 如果拓扑失败
   - 优先看 client/server 输出是否再次出现 path 迁移相关超时
   - 再检查 `example/server.c` 的 preferred socket 收发是否与 `active_path_index` 一致

## 当前值得注意的文件

### 已改且与阶段 5 强相关

- `include/quic_recovery.h`
- `include/quic_tls.h`
- `src/tls/quic_tls.c`
- `tests/test_phase18.c`
- `tests/test_phase19.c`
- `tests/test_phase20.c`
- `example/client.c`
- `example/server.c`
- `topo.py`
- `Makefile`

### 仍可能继续调整

- `example/server.c`
- `README.md`
- `topo.py`

## 当前实现中的临时调试痕迹

`example/client.c` 和 `example/server.c` 里还保留了少量阶段 5 调试输出，例如：

- `client learned preferred address ...`
- `client started preferred-address migration`
- `server handshake complete and preferred-address mode is active`

这些输出在定位阶段 5 真实迁移问题时有用，但在最终收尾前应评估是否保留。

## 一句话状态

阶段 5 的协议核心、单元测试和本机 preferred-address loopback 都已经通过；当前唯一未闭环的是 `make topo-stage5-preferred` 的真实拓扑复验。
